const loginController = require('./login');
const userController = require('./user');
const categoryController = require('./categories');
const blogPostController = require('./post');

module.exports = {
    loginController,
    userController,
    categoryController,
    blogPostController,
};